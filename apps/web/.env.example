# GraphQL endpoint (Strapi)
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:1337/graphql

# Production CMS image hostname (e.g. api.example.com)
NEXT_PUBLIC_CMS_HOSTNAME=
NEXT_PUBLIC_CMS_PROTOCOL=https

# API token for Strapi GraphQL (Settings > API Tokens in admin)
STRAPI_API_TOKEN=

# Revalidate: POST /api/revalidate expects x-forge-revalidate-token header
STRAPI_REVALIDATE_TOKEN=

# Preview: GET /api/preview?token=... enables draft mode
STRAPI_PREVIEW_TOKEN=
