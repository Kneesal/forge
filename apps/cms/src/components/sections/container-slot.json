{
  "collectionName": "components_sections_container_slots",
  "info": {
    "displayName": "Container Slot",
    "icon": "layout",
    "description": "Single slot with grid span and dynamic content (e.g. media collection; text, BibleQuotesCarousel when added in follow-up issues)"
  },
  "options": {},
  "attributes": {
    "gridSpan": {
      "type": "integer",
      "min": 1,
      "max": 12,
      "required": true,
      "default": 6
    },
    "content": {
      "type": "dynamiczone",
      "components": ["sections.media-collection", "sections.text"],
      "required": true
    }
  }
}
